# Dependencies (프로젝트는 no-build 이지만 실수 방지용)
node_modules/
package-lock.json

# Secrets — API 키는 서버에만, 절대 커밋 금지 (.env.example 만 커밋)
.env
server/.env
*.env
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Temp
*.tmp
.cache/
dist/
